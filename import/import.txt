For a site Import:
Make sure the site files are in a folder, and the parent directory is named the same as the local dev site directory (example.loc) and located in this directory.
~/linux-dev/import/
Make sure the sql file is named the same as the local dev site DB and located in this directory.
~/linux-dev/import/
example:
- import
---- example.loc
---- example.sql
