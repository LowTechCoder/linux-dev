    1  sudo apt install tasksel
    2  shutdown now
    3  sudo tasksel
    4  shutdown -r now
    5  sudo apt install spice-vdagent spice-webdavd
    6  sudo install chromium
    7  sudo apt-cache search chrome
    8  sudo apt-cache search chrome | grep chrom
    9  sudo apt-cache search chrom | grep chrom
   10  sudo apt install chromium-browser
   11  sudo apt update
   12  sudo apt upgrade
   13  shutdown now
   14  shutdown -r now
   15  startx
   16  sudo tasksel
   17  shutdown -r now
   18  cd /var/www/
   19  ls
   20  cd html
   21  ls
   22  sudo nvim index.html 
   23  mkdir wordpress
   24  sudo mkdir wordpress
   25  sudo nvim /var/www/html/info.php
   26  sudo apt install phpmyadmin
   27  ls
   28  cd ..
   29  ls
   30  cd html/
   31  wget https://wordpress.org/latest.zip
   32  sudo apt install unzip
   33  sudo mkdir -p /var/www/
   34  sudo unzip latest.zip -d /var/www/
   35  ls
   36  sudo apt install unzip
   37  wget https://wordpress.org/latest.zip
   38  ls
   39  sudo wget https://wordpress.org/latest.zip
   40  sudo unzip latest.zip -d /var/www/
   41  ls
   42  ls wordpress/
   43  ls
   44  mv latest.zip wordpress/
   45  sudo mv latest.zip wordpress/
   46  cd wordpress/
   47  sudo unzip latest.zip
   48  ls
   49  ls wordpress/
   50  cd ..
   51  ls
   52  sudo cp wordpress/latest.zip .
   53  ls
   54  sudo rm -r wordpress/
   55  sudo unzip latest.zip
   56  ls wordpress/
   57  ls
   58  sudo mysql -u root
   59  cd wordpress/
   60  sudo cp wp-config-sample.php wp-config.php
   61  sudo nvim wp-config.php 
   62  sudo chown www-data:www-data /var/www/html/wordpress/ -R
   63  sudo nvim wp-config.php 
   64  sudo systemctl reload apache2
   65  sudo mysql -u root
   66  history
